{
  "name": "remark",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clear": "rm -rf node_modules && npm run clear-dist",
    "clear-dist": "rm -rf dist/*",
    "create-folder-structure": "mkdir -p dist/css && mkdir -p dist/javascript",
    "copy-libs": "cp src/css/* dist/css/ && cp src/javascript/* dist/javascript/",
    "add-external-libs": "cp node_modules/@ajusa/lit/dist/lit.css dist/css/ && cp node_modules/jquery/dist/jquery.min.js dist/javascript/",
    "transform-html-files": "cp src/*.html dist/",
    "build": "npm run clear-dist && npm install && npm run create-folder-structure && npm run copy-libs && npm run add-external-libs && npm run transform-html-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/itsmethemojo/remark.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/itsmethemojo/remark/issues"
  },
  "homepage": "https://github.com/itsmethemojo/remark#readme",
  "dependencies": {
    "@ajusa/lit": "^1.1.0",
    "jquery": "^3.4.1"
  }
}
